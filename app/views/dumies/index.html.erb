<p id="notice"><%= notice %></p>

<h1>Hello world</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @dumies.each do |dumy| %>
      <tr>
        <td><%= link_to 'Show', dumy %></td>
        <td><%= link_to 'Edit', edit_dumy_path(dumy) %></td>
        <td><%= link_to 'Destroy', dumy, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Dumy', new_dumy_path %>
